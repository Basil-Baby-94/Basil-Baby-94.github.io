name: basilbaby
description: "Showcasing My Work: Personal Portfolio Website"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.7
  flutter_animate: ^4.5.0
  google_fonts: ^4.0.3
  url_strategy: ^0.2.0
  uuid: ^3.0.7
  provider: ^6.1.2
  lottie: ^3.1.2
  flutter_hooks: ^0.18.0
  flutter_staggered_animations: ^1.1.1
  url_launcher: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/json/
    - assets/projects/
